function maxSum( arr, n, k){
    // Initialize result
    let max_sum = Number.MIN_VALUE;
 
    console.log(max_sum)
}
 

let arr = [ 1, 4, 2, 10, 2, 3, 1, 0, 20 ];
let k = 4;
let n = arr.length;
maxSum(arr, n, k);